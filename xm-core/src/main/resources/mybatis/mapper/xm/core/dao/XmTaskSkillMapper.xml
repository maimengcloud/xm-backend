<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qqkj.xm.core.entity.XmTaskSkill">
	
	
	<!--开始 自定sql函数区域 -->
		<!--请在此区域添加自定义函数-->
		
		
		
	<update id="updateXmTaskSkillIdsAndNamesByTaskId"
		parameterType="String">
		update XM.xm_task t,(select task_id, group_concat(e.task_skill_id  Separator ',') as  task_skill_ids,group_concat(e.task_skill_name  Separator ',') as task_skill_names from  XM.xm_task_skill e where e.task_id=#{taskId}) e1
		set t.task_skill_ids=  e1.task_skill_ids , t.task_skill_names =  e1.task_skill_names
		 
		where e1.task_id=#{taskId}  and t.id=e1.task_id
	</update>	
	<!--结束 自定义sql函数区域-->
	

	
	<!-- 通过条件查询获取数据列表 返回list<map> -->
	<select id="selectListMapByWhere" parameterType="HashMap" resultType="HashMap">
		select * from XM.xm_task_skill res
		<where> 
			<if test="ids != null">  and     
				id in
				    <foreach collection="ids" item="item" index="index" open="(" separator="," close=")" >  
				        #{item}  
				    </foreach>  
			 </if>
			<include refid="where"/>
			<if test="key != null and key !='' ">    </if>
		</where>
	</select>
	
	<!-- 通过条件查询获取数据列表 不分页 返回 list<Object> -->
	<select id="selectListByWhere" parameterType="com.qqkj.xm.core.entity.XmTaskSkill" resultType="com.qqkj.xm.core.entity.XmTaskSkill">
		select * from XM.xm_task_skill res
		<where>
			<include refid="where"/>
		</where>
	</select>
	
		<!-- 通过主键查询获取数据对象 返回object -->
	<select id="selectOneObject" parameterType="com.qqkj.xm.core.entity.XmTaskSkill" resultType="com.qqkj.xm.core.entity.XmTaskSkill">
		select * from XM.xm_task_skill res
		where 
			res.id = #{id}
	</select>
	
	<!-- 通过主键查询获取数据对象 返回map-->
	<select id="selectOneMap" parameterType="HashMap" resultType="HashMap">
		select * from XM.xm_task_skill res
		where 
			 res.id = #{id}
	</select>
	<!-- 获取数据条目 返回long -->
	<select id="countByWhere" parameterType="com.qqkj.xm.core.entity.XmTaskSkill" resultType="long">
		select count(1) from XM.xm_task_skill res
		<where>
			<include refid="where"/>
		</where>
	</select>
	<!-- 新增一条记录  主键id,-->
	<insert id="insert" parameterType="com.qqkj.xm.core.entity.XmTaskSkill" useGeneratedKeys="false"   keyProperty="id">
		insert into XM.xm_task_skill(
			<include refid="columns"/>
		) values (
		#{id},#{taskId},#{taskSkillId},#{taskSkillName},#{skillRemarks}
		)
	</insert>
	
	<!-- 按条件删除若干条记录-->
	<delete id="deleteByWhere" parameterType="com.qqkj.xm.core.entity.XmTaskSkill">
		delete from XM.xm_task_skill res
		<where>  
			<include refid="where"/>
		</where>
	</delete>
	
	<!-- 按主键删除一条记录-->
	<delete id="deleteByPk" parameterType="com.qqkj.xm.core.entity.XmTaskSkill">
		delete from XM.xm_task_skill 
		where id = #{id} 
	</delete>
	
	<!-- 根据条件修改若干条记录 -->
	<update id="updateSomeFieldByPk" parameterType="com.qqkj.xm.core.entity.XmTaskSkill">
		update  XM.xm_task_skill 
		<set>
			<include refid="someFieldSet"/>
		</set>
		where id = #{id} 
	</update>
	
		<!-- 根据主键修改一条记录 -->
	<update id="updateByPk" parameterType="com.qqkj.xm.core.entity.XmTaskSkill">
		update  XM.xm_task_skill 
		<set>
			<include refid="set"/>
		</set>
		where id = #{id} 
	</update>

	<!-- 批量新增  批量插入 借用insert 循环插入实现
	<insert id="batchInsert" parameterType="List">
 	</insert>
 	-->
 	
	<!-- 批量更新 -->
	<update id="batchUpdate" parameterType="List">   
		<foreach collection="list" item="item" index="index" separator=";" >
   		update  XM.xm_task_skill 
		set 
			<include refid="batchSet"/>
			where id = #{item.id}  
		</foreach> 
	</update>
	<!-- 批量删除 -->
	<delete id="batchDelete" parameterType="List">  
		delete from XM.xm_task_skill 
			where id in
			    <foreach collection="list" item="item" index="index" open="(" separator="," close=")" >  
			        #{item.id }  
			    </foreach>  
	</delete>
	
	
	<!--sql片段 列-->
	<sql id="columns">
		id,task_id,task_skill_id,task_skill_name,skill_remarks
	</sql>
	
	<!--sql片段 动态条件 YYYY-MM-DD HH24:MI:SS-->
	<sql id="where">
		<if test="id != null and id != ''">      and res.id = #{id}     </if>
		<if test="taskId != null and taskId != ''">      and res.task_id = #{taskId}     </if>
		<if test="taskSkillId != null and taskSkillId != ''">      and res.task_skill_id = #{taskSkillId}     </if>
		<if test="taskSkillName != null and taskSkillName != ''">      and res.task_skill_name = #{taskSkillName}     </if>
		<if test="skillRemarks != null and skillRemarks != ''">      and res.skill_remarks = #{skillRemarks}     </if>
	</sql>
	<!--sql片段 更新字段 -->
	<sql id="set">
		task_id = #{taskId},
		task_skill_id = #{taskSkillId},
		task_skill_name = #{taskSkillName},
		skill_remarks = #{skillRemarks}
	</sql>
	<sql id="someFieldSet">
		<if test="taskId != null and taskId != ''"> task_id = #{taskId},     </if>
		<if test="taskSkillId != null and taskSkillId != ''"> task_skill_id = #{taskSkillId},     </if>
		<if test="taskSkillName != null and taskSkillName != ''"> task_skill_name = #{taskSkillName},     </if>
		<if test="skillRemarks != null and skillRemarks != ''"> skill_remarks = #{skillRemarks},     </if>
	</sql>
	<!--sql片段 批量更新 -->
	<sql id="batchSet">
			task_id = #{item.taskId},
			task_skill_id = #{item.taskSkillId},
			task_skill_name = #{item.taskSkillName},
			skill_remarks = #{item.skillRemarks}
	</sql>
</mapper>