<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xm.core.entity.XmAttachment">
	
	
	<!--开始 自定sql函数区域 -->
		<!--请在此区域添加自定义函数-->
	<delete id="deleteByOrigin" parameterType="com.xm.core.entity.XmAttachment">
		delete from xm_attachment where origin_id = #{originId} and origin_type = #{originType}
	</delete>
		
		
	<!--结束 自定义sql函数区域-->
	

	
	<!-- 通过条件查询获取数据列表 返回list<map> -->
	<select id="selectListMapByWhere" parameterType="HashMap" resultType="HashMap">
		select * from xm_attachment res
		<where> 
			<if test="ids != null">  and     
				id in
				    <foreach collection="ids" item="item" index="index" open="(" separator="," close=")" >  
				        #{item}  
				    </foreach>  
			 </if>
			<include refid="where"/>
			<if test="key != null and key !='' ">       </if> 
		</where>
	</select>
	
	<!-- 通过条件查询获取数据列表 不分页 返回 list<Object> -->
	<select id="selectListByWhere" parameterType="com.xm.core.entity.XmAttachment" resultType="com.xm.core.entity.XmAttachment">
		select * from xm_attachment res
		<where>
			<include refid="where"/>
		</where>
	</select>
	
		<!-- 通过主键查询获取数据对象 返回object -->
	<select id="selectOneObject" parameterType="com.xm.core.entity.XmAttachment" resultType="com.xm.core.entity.XmAttachment">
		select * from xm_attachment res
		where 
			res.id = #{id}
	</select>
	
	<!-- 通过主键查询获取数据对象 返回map-->
	<select id="selectOneMap" parameterType="HashMap" resultType="HashMap">
		select * from xm_attachment res
		where 
			 res.id = #{id}
	</select>
	<!-- 获取数据条目 返回long -->
	<select id="countByWhere" parameterType="com.xm.core.entity.XmAttachment" resultType="long">
		select count(1) from xm_attachment res
		<where>
			<include refid="where"/>
		</where>
	</select>
	<!-- 新增一条记录  主键id,-->
	<insert id="insert" parameterType="com.xm.core.entity.XmAttachment" useGeneratedKeys="false" keyProperty="id">
		insert into xm_attachment(
			<include refid="columns"/>
		) values (
		#{id},#{originType},#{originId},#{name},#{addr},#{type},#{remark}
		)
	</insert>
	
	<!-- 按条件删除若干条记录-->
	<delete id="deleteByWhere" parameterType="com.xm.core.entity.XmAttachment">
		delete from xm_attachment res
		<where>  
			<include refid="where"/>
		</where>
	</delete>
	
	<!-- 按主键删除一条记录-->
	<delete id="deleteByPk" parameterType="com.xm.core.entity.XmAttachment">
		delete from xm_attachment
		where id = #{id} 
	</delete>
	
	<!-- 根据条件修改若干条记录 -->
	<update id="updateSomeFieldByPk" parameterType="com.xm.core.entity.XmAttachment">
		update  xm_attachment
		<set>
			<include refid="someFieldSet"/>
		</set>
		where id = #{id} 
	</update>
	
		<!-- 根据主键修改一条记录 -->
	<update id="updateByPk" parameterType="com.xm.core.entity.XmAttachment">
		update  xm_attachment
		<set>
			<include refid="set"/>
		</set>
		where id = #{id} 
	</update>

	<!-- 批量新增  批量插入 借用insert 循环插入实现
	<insert id="batchInsert" parameterType="List">
 	</insert>
 	-->
 	
	<!-- 批量更新 -->
	<update id="batchUpdate" parameterType="List">   
		<foreach collection="list" item="item" index="index" separator=";" >
   		update  xm_attachment
		set 
			<include refid="batchSet"/>
			where id = #{item.id}  
		</foreach> 
	</update>
	<!-- 批量删除 -->
	<delete id="batchDelete" parameterType="List">  
		delete from xm_attachment
			where id in
			    <foreach collection="list" item="item" index="index" open="(" separator="," close=")" >  
			        #{item.id }  
			    </foreach>  
	</delete>
	
	
	<!--sql片段 列-->
	<sql id="columns">
		id,origin_type,origin_id,name,addr,type,remark
	</sql>
	
	<!--sql片段 动态条件 YYYY-MM-DD HH24:MI:SS-->
	<sql id="where">
		<if test="id != null and id != ''">      and res.id = #{id}     </if>
		<if test="originType != null and originType != ''">      and res.origin_type = #{originType}     </if>
		<if test="originId != null and originId != ''">      and res.origin_id = #{originId}     </if>
		<if test="name != null and name != ''">      and res.name = #{name}     </if>
		<if test="addr != null and addr != ''">      and res.addr = #{addr}     </if>
		<if test="type != null and type != ''">      and res.type = #{type}     </if>
		<if test="remark != null and remark != ''">      and res.remark = #{remark}     </if>
	</sql>
	<!--sql片段 更新字段 -->
	<sql id="set">
		origin_type = #{originType},
		origin_id = #{originId},
		name = #{name},
		addr = #{addr},
		type = #{type},
		remark = #{remark}
	</sql>
	<sql id="someFieldSet">
		<if test="originType != null and originType != ''"> origin_type = #{originType},     </if>
		<if test="originId != null and originId != ''"> origin_id = #{originId},     </if>
		<if test="name != null and name != ''"> name = #{name},     </if>
		<if test="addr != null and addr != ''"> addr = #{addr},     </if>
		<if test="type != null and type != ''"> type = #{type},     </if>
		<if test="remark != null and remark != ''"> remark = #{remark},     </if>
	</sql>
	<!--sql片段 批量更新 -->
	<sql id="batchSet">
			origin_type = #{item.originType},
			origin_id = #{item.originId},
			name = #{item.name},
			addr = #{item.addr},
			type = #{item.type},
			remark = #{item.remark}
	</sql>
</mapper>